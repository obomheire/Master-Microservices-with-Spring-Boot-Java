NB: git-local-config-repo, spring-cloud-config-server and limits-service are from the same project. Create a git local repository name "git-local-config-repo" for the installation.
"limits-service" is the microservice while "spring-cloud-config-server" is the configuration server.
